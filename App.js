import { StatusBar } from 'expo-status-bar';
import React from 'react';
import { StyleSheet, Text, View } from 'react-native';
import WelcomeScree from '../screens/WelcomeScreen';

export default function App() {

  return (
    <WelcomeScreen/>
  );
}
// please send code link n mushirahmed.shaikh@whitehatjr.com k
// i will check frmo my eend
const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#fff',
    alignItems: 'center',
    justifyContent: 'center',
  },
});
